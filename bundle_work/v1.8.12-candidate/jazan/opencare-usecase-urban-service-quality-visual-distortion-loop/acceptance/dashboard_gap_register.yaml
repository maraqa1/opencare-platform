kind: DashboardGapRegister
use_case_slug: urban-service-quality-visual-distortion-loop
required: true
rule: Rendered dashboard conformance must produce a gap register for every canonical dashboard; implementation is not accepted while critical or high gaps remain.
dashboards:
- dashboard_id: 01_strategic_objective_monitoring
  route: /jazan-performance/use-cases/urban-service-quality-visual-distortion-loop
  review_status: pending_render_review
  critical_or_high_gaps_allowed: false
  required_gap_fields:
  - gap_id
  - severity
  - required
  - actual
  - violation_type
  - required_component
  - fix_required
  initial_known_risks:
  - same data rendered through weaker generic composition
  - required zone components moved into generic shell
  - bilingual component labels omitted
  - mockup/rendered screenshot not provided
- dashboard_id: 02_kpi_municipality_workspace
  route: /jazan-performance/use-cases/urban-service-quality-visual-distortion-loop/kpi/{kpi_slug}
  review_status: pending_render_review
  critical_or_high_gaps_allowed: false
  required_gap_fields:
  - gap_id
  - severity
  - required
  - actual
  - violation_type
  - required_component
  - fix_required
  initial_known_risks:
  - same data rendered through weaker generic composition
  - required zone components moved into generic shell
  - bilingual component labels omitted
  - mockup/rendered screenshot not provided
- dashboard_id: 03_case_model_intelligence
  route: /jazan-performance/use-cases/urban-service-quality-visual-distortion-loop/case/{case_id}/intelligence
  review_status: pending_render_review
  critical_or_high_gaps_allowed: false
  required_gap_fields:
  - gap_id
  - severity
  - required
  - actual
  - violation_type
  - required_component
  - fix_required
  initial_known_risks:
  - same data rendered through weaker generic composition
  - required zone components moved into generic shell
  - bilingual component labels omitted
  - mockup/rendered screenshot not provided
- dashboard_id: 04_decision_command_centre
  route: /jazan-performance/use-cases/urban-service-quality-visual-distortion-loop/decisions
  review_status: pending_render_review
  critical_or_high_gaps_allowed: false
  required_gap_fields:
  - gap_id
  - severity
  - required
  - actual
  - violation_type
  - required_component
  - fix_required
  initial_known_risks:
  - same data rendered through weaker generic composition
  - required zone components moved into generic shell
  - bilingual component labels omitted
  - mockup/rendered screenshot not provided
- dashboard_id: 05_runtime_evidence_execution_history
  route: /jazan-performance/use-cases/urban-service-quality-visual-distortion-loop/runtimes
  review_status: pending_render_review
  critical_or_high_gaps_allowed: false
  required_gap_fields:
  - gap_id
  - severity
  - required
  - actual
  - violation_type
  - required_component
  - fix_required
  initial_known_risks:
  - same data rendered through weaker generic composition
  - required zone components moved into generic shell
  - bilingual component labels omitted
  - mockup/rendered screenshot not provided
- dashboard_id: 06_decision_action_audit
  route: /jazan-performance/use-cases/urban-service-quality-visual-distortion-loop/audit
  review_status: pending_render_review
  critical_or_high_gaps_allowed: false
  required_gap_fields:
  - gap_id
  - severity
  - required
  - actual
  - violation_type
  - required_component
  - fix_required
  initial_known_risks:
  - same data rendered through weaker generic composition
  - required zone components moved into generic shell
  - bilingual component labels omitted
  - mockup/rendered screenshot not provided
