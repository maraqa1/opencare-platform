name: opencare
version: 0.1.0
config-version: 2

profile: opencare

model-paths:
  - models

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  opencare:
    +materialized: view
    +schema: analytics
    staging:
      +materialized: view
    marts:
      +materialized: table
