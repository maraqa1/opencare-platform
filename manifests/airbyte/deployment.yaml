---
apiVersion: v1
kind: Service
metadata:
  name: airbyte-temporal
  namespace: opencare
spec:
  selector:
    app: airbyte-temporal
  ports:
    - name: grpc
      protocol: TCP
      port: 7233
      targetPort: 7233
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: airbyte-temporal
  namespace: opencare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: airbyte-temporal
  template:
    metadata:
      labels:
        app: airbyte-temporal
    spec:
      containers:
        - name: temporal
          image: temporalio/auto-setup:1.27.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 7233
              name: grpc
          env:
            - name: DB
              value: postgres12
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: opencare-config
                  key: POSTGRES_PORT
            - name: POSTGRES_SEEDS
              valueFrom:
                configMapKeyRef:
                  name: opencare-config
                  key: POSTGRES_HOST
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: opencare-config
                  key: POSTGRES_USER
            - name: POSTGRES_PWD
              valueFrom:
                secretKeyRef:
                  name: opencare-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: opencare-config
                  key: POSTGRES_DB
            - name: DYNAMIC_CONFIG_FILE_PATH
              value: config/dynamicconfig/development-sql.yaml
          readinessProbe:
            tcpSocket:
              port: 7233
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 7233
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: airbyte-server
  namespace: opencare
spec:
  selector:
    app: airbyte-server
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: airbyte-server
  namespace: opencare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: airbyte-server
  template:
    metadata:
      labels:
        app: airbyte-server
    spec:
      containers:
        - name: airbyte-server
          image: airbyte/server:build-8301b7db38-22827-master
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
              name: http
          envFrom:
            - configMapRef:
                name: opencare-config
            - secretRef:
                name: opencare-secrets
          env:
            - name: AIRBYTE_ROLE
              value: server
            - name: AIRBYTE_WORKSPACE_ROOT
              value: /data/workspace
            - name: TEMPORAL_HOST
              value: airbyte-temporal:7233
            - name: STORAGE_TYPE
              value: MINIO
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: opencare-secrets
                  key: MINIO_ACCESS_KEY
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: opencare-secrets
                  key: MINIO_SECRET_KEY
            - name: AWS_DEFAULT_REGION
              valueFrom:
                configMapKeyRef:
                  name: opencare-config
                  key: MINIO_REGION
            - name: S3_PATH_STYLE_ACCESS
              valueFrom:
                configMapKeyRef:
                  name: opencare-config
                  key: AIRBYTE_S3_PATH_STYLE
            - name: S3_MINIO_ENDPOINT
              value: http://minio.opencare.svc.cluster.local:9000
            - name: AIRBYTE_MINIO_ENDPOINT
              value: http://minio.opencare.svc.cluster.local:9000
            - name: MINIO_SSL
              value: "false"
            - name: DATABASE_URL
              value: jdbc:postgresql://postgres:5432/opencare
            - name: DATABASE_USER
              valueFrom:
                configMapKeyRef:
                  name: opencare-config
                  key: POSTGRES_USER
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: opencare-secrets
                  key: POSTGRES_PASSWORD
          readinessProbe:
            tcpSocket:
              port: 8001
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8001
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
          volumeMounts:
            - name: airbyte-data
              mountPath: /data
      volumes:
        - name: airbyte-data
          emptyDir: {}
