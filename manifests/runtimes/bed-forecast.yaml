---
apiVersion: v1
kind: Service
metadata:
  name: bed-forecast
  namespace: opencare
spec:
  selector:
    app: bed-forecast
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bed-forecast
  namespace: opencare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bed-forecast
  template:
    metadata:
      labels:
        app: bed-forecast
    spec:
      containers:
        - name: bed-forecast
          image: ghcr.io/maraqa1/opencare-runtime-bed-forecast:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: opencare-config
            - secretRef:
                name: opencare-secrets
          env:
            - name: PORT
              value: "8000"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: bed-forecast-refresh
  namespace: opencare
spec:
  schedule: "15 * * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: trigger
              image: curlimages/curl:8.12.1
              args:
                - -fsS
                - -X
                - POST
                - http://bed-forecast:8000/run
              resources:
                requests:
                  cpu: 10m
                  memory: 32Mi
                limits:
                  cpu: 50m
                  memory: 64Mi
