# SQL Scaffold

This directory holds bootstrap SQL and shared schema definitions used to initialize the platform data contract.

- `superset/superset_readonly.sql`: grants the Superset read-only role access to the `analytics` schema only
